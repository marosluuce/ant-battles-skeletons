# Skeleton

This is a skeleton for Ant Battling in Elixir.

## Getting Running

  1. Download dependencies:

    ```
    mix deps.get
    ```

  2. Update your config:

    ```elixir
    # config/config.exs

    config :skeleton, server_url: "server address"
    ```

  3. Battle!
